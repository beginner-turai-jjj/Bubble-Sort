# バブルソート（Bubble Sort）

このリポジトリでは、Pythonで実装した **バブルソート** を公開しています。

## 概要

バブルソートは、隣り合う要素を比較して並び替えるシンプルなソートアルゴリズムです。

## アルゴリズムの流れ

1. 隣同士の値を比較
2. 大きい方を後ろに移動
3. これを繰り返すことで最大値が最後に移動
4. 全体が整列されるまで繰り返す

## 計算量

- 平均：O(n²)
- 最悪：O(n²)
- 最良：O(n)

## ファイル
